# Malformed fixture — OAuth/session credentials (spec §79/§23)
# Must be detected and rejected, no Connection imported.
oauth_access_token=fake-oauth-token-not-real
session_token=fake-session-token-not-real
refresh_token=fake-refresh-token-not-real
OPENAI_API_KEY=sk-test-malformed-with-oauth-abcdef
OPENAI_BASE_URL=http://127.0.0.1:18071/v1
