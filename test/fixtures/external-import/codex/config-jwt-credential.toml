# Codex fixture for E2E Case 25 (spec §32) — JWT credential rejection.
# apiKey 值是标准 JWT 结构（三段 base64url），但 payload 含 oauth/scope 字段，
# 应被 credentialClassifier 识别为 oauth_token 并拒绝导入（UNSUPPORTED 状态）。
# 这不是真实凭据 —— 是手工构造的测试 JWT，payload 公开可读。
# payload = {"scope":"openid","oauth":true,"sub":"test-user"} （base64url 编码）
model = "model-jwt"
model_provider = "jwttest"

[model_providers.jwttest]
name = "JWT Credential Provider"
base_url = "http://127.0.0.1:18080/v1"
wire_api = "chat"
api_key = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzY29wZSI6Im9wZW5pZCIsIm9hdXRoIjp0cnVlLCJzdWIiOiJ0ZXN0LXVzZXIifQ.SflKxwRJSMeKKF2QT4fwpMeJf36POk6yJV_adQssw5c"
